module.exports = function() {
    var root = './';
    var server = './server/';
    var test   = './test/';

    var config = {
        jsfiles: [
            root + '*.js',
            server + '**/*.js',
            test + '**/*.js'
        ],
        root: root,
        server: server,
        test: test
    };

    return config;
};
